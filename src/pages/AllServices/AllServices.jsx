import { useLoaderData } from "react-router-dom";

const AllServices = () => {
    const services = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default AllServices;